I plan to make the app more powerful by adding functionality to have multiple "sheets", wheras every sheet holds its own Filter/Color/Size/XY-Axes configuration. Sheets should be selectable by tabls at the bottom (if you have a better idea let me know). Every sheet is an own instance of a plot including all required parameters.
Is that possible ?
Additionally in a second step I plan for shared filters across sheets.
